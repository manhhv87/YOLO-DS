runs/
clean.bat

# Generated datasets (rebuilt by train/data/*.py — do not commit).
# Glob covers per-seed variants (dataset_synth_s1/, ...); does NOT match the
# committed datasets/ dir (no underscore after "dataset").
dataset_*/

# Python caches
__pycache__/
*.pyc

# Auto-downloaded model weights
*.pt

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.blg
paper/*.bbl
paper/*.fls
paper/*.fdb_latexmk
